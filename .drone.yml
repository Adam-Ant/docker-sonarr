---
kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: build
  pull: always
  image: spritsail/docker-build

- name: test
  pull: always
  image: spritsail/docker-test
  settings:
    curl: :8989/api/system/status
    curl_opts: --header x-api-key:drone
    pipe: |
      jq -r ".version" |
      grep -qw "$(label io.spritsail.version.sonarr)"
    run_args: -t -e SUID=0 -e API_KEY=drone
    verbose: true

- name: publish
  pull: always
  image: spritsail/docker-publish
  settings:
    login: {from_secret: docker_login}
    repo: spritsail/sonarr
    tags:
    - beta
    - "%label io.spritsail.version.sonarr | %prefix beta | %auto"
  when:
    branch:
    - beta
    event:
    - push

---
kind: signature
hmac: 43cce35893e96e582a96d5ae92ae15867991f9cbb6fa2b4101b10ae5106affdd

...
